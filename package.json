{
  "name": "@benkeil/typescript-usecase",
  "version": "0.0.11",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "test": "jest",
    "prebuild": "yarn run clean && yarn run test",
    "build": "tsc",
    "prepublishOnly": "yarn run build"
  },
  "peerDependencies": {
    "rxjs": "^7.1.0"
  },
  "devDependencies": {
    "@benkeil/typescript-result": "0.0.3",
    "@types/jest": "^26.0.23",
    "jest": "^27.0.4",
    "prettier": "^2.3.1",
    "rxjs": "^7.1.0",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.2"
  }
}
