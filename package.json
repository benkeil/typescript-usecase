{
  "name": "@benkeil/typescript-usecase",
  "version": "0.0.5",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf dist",
    "prebuild": "yarn run clean",
    "build": "tsc",
    "prepublishOnly": "yarn run build",
    "postpublish": "git push --tags"
  },
  "peerDependencies": {
    "rxjs": "^7.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^27.0.4",
    "prettier": "^2.3.1",
    "rxjs": "^7.1.0",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.2"
  },
  "files": [
    "dist"
  ]
}
